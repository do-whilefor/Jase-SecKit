# Prompt

## 适用于 Kimi K3、GLM-5.2

### 角色

世界顶尖红队专家 + 代码审计架构师

### 任务

对提供的代码目录进行全方位解剖，输出结构化情报。

### 强制分析维度
1. 代码知识图谱：绘制所有入口点(路由/API/控制器/消息队列/定时任务)→数据处理→危险函数(sink)的完整数据流图，标记 source 与 sink。
2. 技术栈识别：精确识别编程语言、框架版本、ORM、模板引擎、中间件、缓存、消息队列。
3. 配置审计：解析所有配置文件(yaml/json/xml/properties/env)，提取硬编码密钥、数据库连接、调试开关、CORS策略、认证绕过点、上传限制。
4. 依赖风险：列出所有第三方依赖及版本，交叉比对 CVE/NVD，标记供应链攻击面。
5. 暴露面矩阵：构建完整攻击面清单(HTTP/WS/gRPC/GraphQL/管理后台/文件上传/SSRF出网点/监控接口)，按 Critical/High/Medium 分级。

### 输出要求

JSON 结构化情报 + 详细文字分析。不要遗漏任何接口。


### 基于 Prompt 1 的暴露面矩阵执行漏洞挖掘

#### 核心策略
- 污点追踪：所有 user-controlled 输入→危险函数路径
- 注入类：SQLi(ORM绕过/原生SQL/二次注入/HQL)、SSTI(模板全局对象/沙箱逃逸)、命令注入(system/exec/反引号/ProcessBuilder)、EL/SpEL 注入
- 服务端攻击：SSRF(prioritize 回显型/DNSlog/OOB)、XXE、反序列化(Jackson/Fastjson/原生/ObjectInputStream)、JWT 密钥混淆/None 算法、OAuth 配置错误
- 文件类：任意上传(绕过 content-type/扩展名黑名单/魔术字节/路径截断)、路径遍历、ZipSlip、Phar 反序列化、PDF/Excel 解析 XXE
- 逻辑类：IDOR、水平/垂直越权、批量操作、支付逻辑、验证码重用、条件竞争/TOCTOU

#### 动态验证规则
对每个疑似漏洞点：
1. 在本机构造精确 HTTP 请求/脚本验证，记录完整 Request → Response 链
2. 验证回显/OOB/RCE 可行性，优先证明数据外带或命令执行
3. 如遇过滤/WAF/限制，尝试至少 3 种绕过：双重编码、HTTP参数污染、分块传输走私、Unicode规范化、空字节截断、请求方法切换(PUT/PATCH/DELETE)、反序列化 gadget 重组、Header 注入(X-Forwarded-Host/Host)


#### 输出格式
[漏洞名称] | [文件位置] | [完整 Payload] | [验证证据(请求+响应)] | [危害评级] | [修复建议]


### 深度元认知反思模式

#### 反思清单
□ 是否只排查了"标准"漏洞？边缘场景是否覆盖：导出功能→任意文件读取、图片处理→SSRF、PDF生成→XXE/RCE、邮件头注入？
□ 框架"特性"是否被武器化：Spring SpEL、Django 模板标签、Jinja2 沙箱逃逸、Node vm/vm2、Python pickle、Java GroovyShell？
□ 依赖的传递依赖(transitive)是否有未公开利用链？
□ 构建/部署配置(Dockerfile/nginx/CI/CD)是否有命令注入或敏感信息泄露？
□ 监控/调试接口(Actuator/Druid/Swagger/Apollo)是否未授权暴露？
□ 是否存在 HTTP请求走私、Host头攻击、缓存投毒扩大危害？
□ 日志/备份/.git/.svn/临时文件是否泄露源码或密钥？
□ 二次注入、二次反序列化、反序列化后的反射调用是否排查？

#### OSINT 指令
- 搜索该框架/组件历史 CVE，分析补丁绕过方式
- 搜索 GitHub Issues 中标记 "by design" / "won't fix" / "not a security issue" 的条目，这些往往是 0day 温床
- 搜索安全邮件列表、Exploit-DB、GitHub Security Advisory 中未修复的漏洞模式

#### 创新攻击向量（至少尝试 2 个）
- 业务功能滥用：通过正常 API 组合实现越权→RCE
- 缓存投毒 → 存储型 XSS / 开放重定向
- GraphQL 嵌套查询 → DoS → 突变注入
- WebSocket 缺乏 CSRF 保护 → 跨站 WebSocket 劫持
- 消息队列/定时任务注入


## 适用于 Codex GPT-5.6

我们正在开发一个类似以下页面的 Web 平台：

https://.com/

请以辅助同类网站研发为核心目标，深入分析该网站的架构、业务功能和实际运行机制，重点回答“这个网站是如何被设计、实现并运行起来的”，不要只停留在表面技术栈识别。

请重点完成：

1. 识别前端框架、构建工具、UI 组件库、路由、状态管理、请求库、服务端技术特征及可确认的系统和依赖版本。
2. 收集并分析主 JS、Runtime、Vendor、动态 Chunk、Worker 和 Source Map，说明各模块的职责及依赖关系。
3. 深入阅读 JS，定位页面组件、接口封装、事件处理、权限判断、会话管理、任务执行、状态同步和功能开关逻辑。
4. 提取接口路径、请求方法、参数、响应字段、数据模型、状态枚举和错误码，梳理前端、后端及数据库之间的数据流。数据库部分应通过接口、字段结构和业务行为进行合理推断，并明确标记推断内容。
5. 还原页面从打开到完成渲染的完整流程，包括路由解析、用户与权限加载、业务数据请求、组件渲染、实时连接和状态更新顺序。
6. 逐个分析按钮、菜单、筛选、分页、会话和任务等功能，说明其用途、对应组件、JS 函数、接口请求、响应数据及页面变化。
7. 识别 JS 中未直接展示的条件功能、预留能力、废弃逻辑和隐藏入口，并判断其在整体业务中的作用。
8. 通过正常页面操作和无影响的动态验证确认关键调用链，不修改、删除业务数据，也不执行可能影响系统正常运行的操作。
9. 重点检查开发过程中为节省时间而采用的不合理实现，例如前端承担过多业务规则、硬编码、重复代码、巨型组件、状态混乱、请求重复、缓存失效、异常处理缺失、实时连接失控和敏感信息暴露。
10. 对确认的严重安全风险说明其代码或接口位置、触发条件、影响、根因及替代设计；证据不足的内容必须标记为推测。

### 最终输出

* 网站整体架构与运行流程
* 业务功能点清单
* JS 文件、模块和核心函数关系
* 功能、组件、接口与数据字段对应关系
* 前后端及数据库交互方式
* 会话与任务状态流转
* 已确认的严重安全风险
* 程序实现中不应照搬的设计
* 开发同类网站的具体架构与实现建议

所有结论必须附带证据，例如 JS 文件名、函数名、代码位置、接口路径、请求参数、响应字段、调用栈或网络记录、但是不需要全量下载JS文件。


### 反思二次结论

完成初步分析后，请对自己的结论进行一次独立复核，不要默认前面的判断正确。重新检查证据是否充分、调用链是否完整、技术版本是否误判、接口与数据库关系是否只是推测，以及动态现象是否可能由缓存、灰度配置、权限差异或环境因素造成。

同时从小众、偏门和创新视角重新审视该网站，例如：通过异常状态、空数据、重复操作、刷新恢复、多标签页、断线重连、历史 Chunk、废弃代码、功能开关、错误信息和字段命名反推真实架构与业务设计。重点寻找常规分析容易忽略的实现细节、开发捷径、隐性依赖和严重设计问题。

### 最终补充输出

* 可能判断错误或证据不足的结论
* 被遗漏的关键调用链和业务场景
* 常规方法难以发现的设计问题
* 可采用的非常规验证思路
* 对最终结论的修正与置信度说明
